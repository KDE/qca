
SET(QCA_CYRUSSASL_SOURCES qca-cyrus-sasl.cpp)

MY_AUTOMOC( QCA_CYRUSSASL_SOURCES )

ADD_LIBRARY(qca-cyrus-sasl SHARED ${QCA_CYRUSSASL_SOURCES})

INCLUDE_DIRECTORIES(${SASL2_INCLUDE_DIR})

TARGET_LINK_LIBRARIES(qca-cyrus-sasl ${QT_QTCORE_LIBRARY} qca ${SASL2_LIBRARIES})

INSTALL(TARGETS qca-cyrus-sasl LIBRARY DESTINATION ${qca_PLUGINSDIR})

